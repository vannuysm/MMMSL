@model EditTeamModel

@{
    ViewData["Title"] = "Create a New Team";
}

<form asp-action="Create" method="post">
    <div class="form-group row" bs-validation-for="Team.DivisionId">
        <label asp-for="Team.DivisionId" class="col-sm-2 col-form-label">Division</label>
        <div class="col-sm-10">
            <select asp-for="Team.DivisionId" asp-items="Model.Divisions" class="form-control" required="required"></select>
        </div>
    </div>

    <div class="form-group row" bs-validation-for="Team.Name">
        <label asp-for="Team.Name" class="col-sm-2 col-form-label">Name</label>
        <div class="col-sm-10">
            <input asp-for="Team.Name" class="form-control" placeholder="Name" required="required" />
        </div>
    </div>

    <div class="form-group row">
        <div class="offset-sm-2 col-sm-10">
            <button type="submit" class="btn btn-success">
                <span class="icon icon-plus"></span> Create
            </button>
        </div>
    </div>
</form>